language: python
python:
  - 3.7
  - 3.8
cache: pip

before_install:
  - python --version
  - pip install -U pip
  - pip install -U flask
  - pip install requests
  - pip install sqlalchemy
  - pip install flask_sqlalchemy
  - pip install flake8 codecov pytest pytest-cov
  
script: 
  - flake8 --ignore=W291 --exclude=freefree/app/web/__init__.py freefree/
    
  - python -m pytest tests --cov freefree/app

after_success:
   - codecov

        

  
